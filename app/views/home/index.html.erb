<div class='well-center'>
  <h1>Client ONE</h1>

  <% if session[:access_token] %>
    <p>You are log in as <%= @owner['email']%></p>
    <p><%= link_to 'Client-Two', ENV['CLIENT_TWO_URL'] %></p>
  <% else %>
    <%= link_to 'Login', '/refresh' %>
  <% end %>
</div>
